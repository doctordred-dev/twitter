# 🔧 Uploadcare Troubleshooting - Фотки не відображаються

## 🔴 Проблема

Фотографії в ленті повертають **404 Not Found**. Це означає, що файли **не зберігаються** в Uploadcare.

## ✅ Що я виправив

### 1. Оновлено `src/services/uploadcare.service.ts`

**Було:**
```typescript
store: 'auto'
```

**Стало:**
```typescript
store: true  // ВАЖЛИВО: зберігати файл постійно
```

### 2. Додано детальне логування

Тепер при завантаженні файлу ти побачиш в консолі:
```
🔄 Starting file upload to Uploadcare...
📁 Filename: post-123456.jpg
🔑 Public Key: 1d3cc966f8...
📦 File size: 245678 bytes
✅ File uploaded successfully!
🆔 UUID: dd6b1dab-a3f4-4872-afe2-9ef8df7b6f00
🔗 CDN URL: https://ucarecdn.com/dd6b1dab-a3f4-4872-afe2-9ef8df7b6f00/
🔍 Verifying file accessibility...
✅ File verification status: 200
```

## 🔍 Що потрібно перевірити

### 1. Перевір Uploadcare Dashboard

1. Зайди на https://app.uploadcare.com/
2. Вибери свій проект
3. Перейди в **Files**
4. Перевір чи є там файли

**Якщо файлів немає** - проблема з завантаженням.

### 2. Перевір Public Key

1. В Uploadcare Dashboard: **Settings → API Keys**
2. Скопіюй **Public Key**
3. Порівняй з `.env`:
   ```env
   UPLOADCARE_PUBLIC_KEY=твій_ключ_тут
   ```

**Важливо:** Ключ має бути **без лапок** і **без пробілів**.

### 3. Перевір чи проект активний

В Uploadcare Dashboard перевір:
- Чи проект не заблокований
- Чи не вичерпано квоту (Free plan: 3000 файлів)
- Чи не вимкнено API

### 4. Перезапусти сервер

Після зміни `.env` **обов'язково** перезапусти:
```bash
npm run dev
```

## 🧪 Тестування

### Тест 1: Створи новий пост з фото

1. Перезапусти сервер
2. Створи пост з фото через фронтенд
3. Подивись логи в консолі сервера
4. Має бути:
   ```
   🔄 Starting file upload to Uploadcare...
   ✅ File uploaded successfully!
   🔗 CDN URL: https://ucarecdn.com/{uuid}/
   ```

### Тест 2: Перевір URL напряму

Скопіюй URL з логів та відкрий в браузері:
```
https://ucarecdn.com/{uuid}/
```

**Якщо 404** - файл не зберігся в Uploadcare.

### Тест 3: Запусти тестовий скрипт

```bash
node test-uploadcare.js
```

Має бути:
```
✅ Upload successful!
✅ File is accessible!
```

## 🐛 Можливі проблеми та рішення

### Проблема 1: "Uploadcare is not configured"

**Причина:** Ключі не задані в `.env`

**Рішення:**
```env
UPLOADCARE_PUBLIC_KEY=твій_public_key
UPLOADCARE_SECRET_KEY=твій_secret_key
```

Перезапусти сервер.

### Проблема 2: Файли завантажуються, але 404

**Причина:** `store: false` або `store: 'auto'` не зберігає файли

**Рішення:** Вже виправлено на `store: true`

### Проблема 3: "UPLOADCARE_PUB_KEY is required"

**Причина:** Невірний формат Public Key

**Рішення:**
1. Перевір що ключ правильний
2. Перевір що немає зайвих символів
3. Перевір що `.env` файл в корені проекту

### Проблема 4: Квота вичерпана

**Причина:** Free plan має ліміт 3000 файлів

**Рішення:**
1. Видали старі файли в Uploadcare Dashboard
2. Або апгрейдни план

### Проблема 5: Старі пости з 404

**Причина:** Старі пости створені до виправлення

**Рішення:**
1. Старі пости не виправити (файли не існують)
2. Створи нові пости - вони працюватимуть

## 📝 Чеклист

- [ ] `.env` файл містить правильні ключі
- [ ] Сервер перезапущено після зміни `.env`
- [ ] В логах видно "Starting file upload to Uploadcare..."
- [ ] В логах видно "File uploaded successfully!"
- [ ] UUID файлу видно в логах
- [ ] Файл доступний за URL `https://ucarecdn.com/{uuid}/`
- [ ] В Uploadcare Dashboard видно завантажені файли

## 🚀 Наступні кроки

1. **Перевір `.env`** - правильні ключі?
2. **Перезапусти сервер** - `npm run dev`
3. **Створи новий пост** з фото
4. **Подивись логи** - чи є помилки?
5. **Перевір Uploadcare Dashboard** - чи з'явився файл?

## 💡 Додаткова інформація

### Де взяти ключі?

1. https://app.uploadcare.com/
2. Вибери проект
3. Settings → API Keys
4. Скопіюй:
   - **Public Key** (demopublickey)
   - **Secret Key** (demosecretkey)

### Формат .env

```env
# Uploadcare
UPLOADCARE_PUBLIC_KEY=1d3cc966f810f853ded2
UPLOADCARE_SECRET_KEY=a99272820653e1f6ac46
```

**Без лапок, без пробілів!**

### Перевірка що ключі завантажились

Додай в `src/index.ts`:
```typescript
console.log('Uploadcare Public Key:', process.env.UPLOADCARE_PUBLIC_KEY?.substring(0, 10) + '...');
```

При старті сервера має бути:
```
Uploadcare Public Key: 1d3cc966f8...
```

## 📞 Якщо нічого не допомагає

1. Перевір чи проект Uploadcare активний
2. Спробуй створити новий проект в Uploadcare
3. Перевір чи не заблоковано API ключі
4. Напиши в Uploadcare Support

---

**Статус:** Код виправлено, тепер потрібно перевірити налаштування Uploadcare.
