# 📸 Uploadcare Integration Guide

## ✅ Что сделано

Проект теперь поддерживает **облачное хранилище Uploadcare** для изображений (посты и аватары).

### Преимущества Uploadcare:
- ✅ CDN для быстрой загрузки изображений по всему миру
- ✅ Автоматическая оптимизация изображений
- ✅ Трансформации на лету (resize, crop, format conversion)
- ✅ Не нужно хранить файлы на сервере
- ✅ Масштабируемость без дополнительной настройки

---

## 🔧 Настройка

### 1. Получи API ключи

1. Зайди в свой проект на [Uploadcare Dashboard](https://uploadcare.com/dashboard/)
2. Перейди в **Settings → API Keys**
3. Скопируй:
   - **Public Key** (например: `demopublickey`)
   - **Secret Key** (например: `demosecretkey`)

### 2. Добавь ключи в `.env`

Создай файл `.env` (если его нет) и добавь:

```env
# Uploadcare
UPLOADCARE_PUBLIC_KEY=your_public_key_here
UPLOADCARE_SECRET_KEY=your_secret_key_here
```

**Важно:** Без этих ключей проект будет использовать локальное хранилище в `/uploads`.

### 3. Перезапусти сервер

```bash
npm run dev
```

При запуске ты увидишь:
- ✅ Если ключи настроены: файлы загружаются в Uploadcare
- ⚠️ Если ключей нет: предупреждение и fallback на локальное хранилище

---

## 📡 Как это работает

### Загрузка изображений

**Посты:**
```bash
POST /posts
Content-Type: multipart/form-data

text: "Текст поста"
image: <File>
```

**Аватары:**
```bash
PATCH /users/me
Content-Type: multipart/form-data

displayName: "Имя"
bio: "Био"
avatar: <File>
```

### Процесс загрузки:

1. **Клиент** отправляет файл на сервер (FormData)
2. **Multer** принимает файл в память (memory storage)
3. **Сервер** загружает файл в Uploadcare через API
4. **Uploadcare** возвращает CDN URL: `https://ucarecdn.com/{uuid}/`
5. **Сервер** сохраняет URL в базу данных
6. **Клиент** получает URL и отображает изображение

### Fallback на локальное хранилище:

Если Uploadcare недоступен или не настроен:
- Файлы сохраняются в `/uploads` на сервере
- URL: `http://localhost:3000/uploads/filename.jpg`

---

## 🎨 Трансформации изображений (опционально)

Uploadcare поддерживает трансформации на лету через URL:

### Примеры:

**Оригинал:**
```
https://ucarecdn.com/{uuid}/
```

**Resize до 300x300:**
```
https://ucarecdn.com/{uuid}/-/resize/300x300/
```

**Crop + качество:**
```
https://ucarecdn.com/{uuid}/-/crop/500x500/center/-/quality/smart/
```

**WebP формат:**
```
https://ucarecdn.com/{uuid}/-/format/webp/
```

### Использование на фронтенде:

```typescript
// Оригинал
const originalUrl = post.imageUrl; // https://ucarecdn.com/{uuid}/

// Превью для ленты (300x300)
const thumbnailUrl = `${originalUrl}-/resize/300x300/-/quality/smart/`;

// Полный размер для модального окна
const fullUrl = `${originalUrl}-/resize/1200x/-/quality/best/`;
```

---

## 🔒 Безопасность

### Public Key
- ✅ Можно использовать на фронтенде
- ✅ Используется только для загрузки файлов
- ✅ Не даёт доступа к удалению или управлению

### Secret Key
- ⚠️ НИКОГДА не передавай на фронтенд
- ⚠️ Используется только на сервере
- ⚠️ Даёт полный доступ к проекту

---

## 📊 Мониторинг

### Проверка загрузок:

1. Зайди в [Uploadcare Dashboard](https://uploadcare.com/dashboard/)
2. Перейди в **Files**
3. Увидишь все загруженные файлы с метаданными

### Статистика:

- Количество файлов
- Использованное место
- Трафик CDN
- Количество запросов

---

## 🚀 Оптимизация для production

### 1. Настрой CORS для Uploadcare

В `.env`:
```env
CORS_ORIGIN=https://your-frontend.com,https://ucarecdn.com
```

### 2. Включи автоматическую оптимизацию

В `uploadcare.service.ts` можно добавить параметры:

```typescript
const result = await client.uploadFile(file, {
  fileName: filename,
  contentType: 'auto',
  store: 'auto', // Автоматическое хранение
  metadata: {
    source: 'twitter-clone',
    uploadedBy: userId
  }
});
```

### 3. Настрой webhooks (опционально)

Uploadcare может отправлять webhooks при загрузке файлов:
- Создай endpoint `POST /webhooks/uploadcare`
- Настрой webhook в Uploadcare Dashboard
- Обрабатывай события (file.uploaded, file.deleted)

---

## 🐛 Troubleshooting

### Ошибка: "Uploadcare is not configured"

**Причина:** Не заданы переменные окружения.

**Решение:**
```bash
# Проверь .env
cat .env | grep UPLOADCARE

# Должно быть:
UPLOADCARE_PUBLIC_KEY=your_key
UPLOADCARE_SECRET_KEY=your_key
```

### Ошибка: "Failed to upload file to Uploadcare"

**Причина:** Неверные ключи или проблемы с сетью.

**Решение:**
1. Проверь ключи в Uploadcare Dashboard
2. Проверь интернет-соединение
3. Проверь логи: `console.error('Uploadcare upload error:', error)`

### Файлы загружаются локально вместо Uploadcare

**Причина:** Ключи не заданы или заданы неправильно.

**Решение:**
1. Перезапусти сервер после добавления ключей
2. Проверь, что ключи без кавычек и пробелов
3. Проверь логи при старте сервера

---

## 📚 Дополнительные ресурсы

- [Uploadcare Documentation](https://uploadcare.com/docs/)
- [Upload API](https://uploadcare.com/docs/api_reference/upload/)
- [Image Transformations](https://uploadcare.com/docs/transformations/image/)
- [REST API](https://uploadcare.com/docs/api_reference/rest/)

---

## ✨ Готово!

Теперь все изображения (посты и аватары) загружаются в Uploadcare CDN. 

**Проверь:**
1. Создай пост с изображением
2. URL должен быть: `https://ucarecdn.com/{uuid}/`
3. Изображение доступно по всему миру через CDN

**Fallback:**
Если Uploadcare недоступен, автоматически используется локальное хранилище.
